#include <stdio.h>
#include <vector>
#include <algorithm>

int main()
{
    int i, j, n, res = 0;
    scanf("%d", &n);
    int met[n][n];
    for (i = 0; i < n; i++)
        for (j = 0; j < n; j++)
            scanf("%d", &met[i][j]);
    for (i = 0; i < n; i++)
        for (j = 0; j < n; j++)
        {
            if (i != 0)
                if (met[])
                    ;
        }
    // 풀다가 문득 왠지 브루트포스에 가까운 방법으로 풀 것 같다는 느낌이 들어서 영상을 봤는데,
    // 아니나 다를까 역시나 그렇게 풀고 있기에 그냥 넘어가기로 했다.
    return 0;
}
